// Entrypoint para a Hostinger (ESM wrapper).
// Executa o servidor compilado em dist/server.cjs.
console.log("[ConsulAI] Iniciando o servidor pelo entrypoint principal (server.js)...");
import './dist/server.cjs';

